import ScoreCounter from "./components/ScoreCounter";

const App = () => {
  return (
    <main className="min-h-screen bg-slate-950 p-8">
      <ScoreCounter />
    </main>
  );
};

export default App;
